{% extends "base.html.twig" %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center mt-5">
            <div class="col col-12 col-md-8 col-lg-6 col-xl-8">
                {{ form_start(form) }}
                <div class="card">
                    <div class="card-header">Mot de passe oublié ?</div>
                    <div class="card-body">
                        {{ form_rest(form) }}
                    </div>
                    <div class="card-footer flex justify-content-end">
                        <button class="btn btn-primary">Continuer</button>
                    </div>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}