{% extends "base.html.twig" %}

{% block body %}
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_rest(form) }}
    <button>Créer une catégorie</button>
    {{ form_end(form) }}
{% endblock %}
